# 本会话进度与结论报告

**日期**：2026-09-24
**范围**：DriveVA-lite 的 Route A（Dynamic Select）重训路线：实现、验证、真实模型接入、正式训练启动
**报告对象**：项目负责人

---

## 一、一句话总结

Route A 从"只有计划文档"做到了"能在真实 DriveVA 上跑通正式训练"，过程中发现并修掉了 **两个会让长跑静默报废的关键缺陷**；同时用一次对照实验**推翻了我自己中途给出的一个结论**。目前训练正在正常进行，但**是否真正有效（PDM 能不能过关）还没有答案**。

---

## 二、术语先对齐（方便不懂细节的人读）

| 词 | 通俗解释 |
|---|---|
| **token** | 模型处理视频时把每一小块画面变成一个"词"，默认一条序列有 1569 个 |
| **history / future token** | 过去 5 帧对应的（780 个）和未来 8 帧对应的（780 个） |
| **PDM** | 自动驾驶规划评分，越高越好；本项目基线 NoPress = **0.9098** |
| **近无损门槛** | 项目定的合格线：PDM 下降的 95% 置信区间下界 > −0.002（即"确实没变差"） |
| **prune（剪枝/选择）** | 从原有 token 里**挑一部分留下**，其余删掉（Route A 走这条） |
| **register（注册向量）** | 不挑原 token，而是**把多个 token 合并成新的向量**（Route B 走这条） |
| **STE** | 让"取/不取"这种硬选择也能参与训练的一种数学技巧 |
| **backbone / DiT** | 模型主体，30 层；"冻结"=不训练它，"解冻"=允许改它 |

---

## 三、这次具体做了什么

### 1. 阅读与交底
读完 `AGENTS.md`（167 KB 交接文件）与 plan v2（2515 行）。结论是：**之前"在冻结模型里找可删 token"这条路已经被判决失败**，plan 要求改成"训练模型去用更少的 token"。所以本次工作全部围绕这个新方向。

### 2. 代码版本化与上传
先把上一会话遗留的改动（DiT 逐层探针结论、按轮次调度 selector 等）提交并推送，再提交本次的 Route A 工作。

### 3. 实现 Route A（新代码包，未改动任何既有模块）
新建 `videopress_framework/videopress/retraining/`，共 7 个模块。用大白话讲每一块干什么：

| 模块 | 干什么 |
|---|---|
| `threshold_gate.py` | **开关**：给每个 token 打 0~1 分，超过阈值就留、否则删。保留数量由数据自己决定，不预设固定值 |
| `dynamic_selector.py` | **打分器**：看 token 本身 + 动作意图 + 当前噪声程度 + 位置，判断它重不重要 |
| `dense_recovery.py` | **还原器**：视频训练目标要求每个位置都有输出，但它只剩少数 token，所以要把少数 token 还原成完整网格 |
| `distillation.py` | **向老师学习**：让新模型模仿原 DriveVA 的输出（轨迹、视频、中间层） |
| `compression_stats.py` | **体检**：统计每条序列到底留了多少 token，并检查"保留数量是否真的随场景变化"（如果所有场景都留差不多，说明这套机制失效了） |
| `route_a.py` | **总装**：把上面几块串成一次完整前向 |
| `curriculum.py` | **课表**：A0→A4 分阶段训练，压缩起点从第 18 层逐步提前到 15、12 层 |

配套：训练/成本脚本 3 个，**59 个新单元测试**（加上原有共 **323 个测试全部通过**）。

### 4. 成本估算（回答"这事值不值得做"）
在真实规模下，如果平均只留 240 个 token（序列长度降到 15.9%）：

| 压缩起点 | 能省多少模型计算 |
|---|---:|
| 第 18 层（Route A 计划起点） | **33.9%** |
| 第 15 层 | **42.4%** |
| 第 12 层 | **50.9%** |

**对比**：目前实际部署的方案只省 2.8–3.4%。所以如果 Route A 成立，收益大一个数量级——这是值得投入的理由。

### 5. 发现两个关键问题

**问题一（致命且隐蔽）：按常规写法，被删掉的 token 收不到任何训练信号。**

计划文档写的是"`V_sparse = V[mask]`"，也就是按 mask 把 token 挑出来。这是教科书式写法，但它有一个后果：token 一旦被剔除，它就**从计算图里消失了**，损失函数无法把"这个 token 该不该留"的信息传回给打分器。

实测（这是硬数据）：

```
保留下来的 token 梯度: 0.0066036
被删掉的   token 梯度: 0.0        ← 完全为零
```

后果是**不可逆的腐蚀**：只有当前被保留的候选在被训练，一个被误删的重要 token 永远无法把分数涨回阈值以上。30 层仿真实测正是如此——第 8 步时保留集里有 72.5% 是正确的关键 token，之后一路衰减到 **0**，保留数量被钉死在保险下限 40。

**问题二：修复方案。** 训练时不用"挑出来"的方式，而是保留整条序列、把被删 token 的值置零（数学上前向结果一样，但每个候选都能拿到梯度）。代码里是 `physical_shortening=False`，训练脚本参数 `--dense-gate`。

配对对照（同一配置、同一课表，只有这一点不同）：

| | 常规写法（gather） | 置零写法（dense-gate） |
|---|---:|---:|
| 保留数量走势 | 384 → 崩到 40 | 稳定在 384 |
| 关键 token 命中率 | 0.047 → **0.000** | 0.047 → **0.074** |
| 轨迹误差 | 0.002841 | **0.001417**（好一半） |

结论：置零写法**阻止了崩溃**，但**它本身也没有让打分器学会选择**（0.074 约等于瞎猜）。

### 6. 一个必须说明的负面结果
我一度以为仿真证明了"打分器学会了挑关键 token"（第 8 步 72.5%）。补做了**未训练对照**后发现不能这么说：我把"关键 token"设计成幅度更大，而任何排序都会偏向幅度大的 token。实测未训练的打分器在同样预算下命中率就有 **0.328**（远高于 7.7% 的随机水平）。

所以：
- 仿真能证明的只是**机制是对的**（前向、反向、开关、还原器、损失都能跑）；
- **不能证明**这套目标函数能学会挑 token；
- 真正站得住的成果是上面那个"梯度为零"的缺陷及其修复。

### 7. 在真实模型上跑通（第一次）
把 Route A 接到真实 DriveVA 的推理路径上，用发布权重在真实场景上跑通，得到 `VALID_RECORD=True`（真实 dim=3072、30 层、bf16、序列 1569、新增 1.775 亿参数）。

跑起来之后才暴露三个**只有真实模型才会出现**的问题，全部修掉：

| 问题 | 后果 |
|---|---|
| 开关算的是 fp32，而主体是 bf16，相乘把整条序列变成 fp32 | 后面每一层都报数据类型不匹配 |
| DriveVA 传的"噪声程度"是**逐 token**的（过去帧=0、未来帧=σ），不是逐条序列 | 打分器直接报错。修成原生支持后反而更好：每个 token 知道自己有多"噪" |
| 位置坐标表没有加 batch 维 | 还原器索引时报维度错误。合成测试恰好都显式传了坐标，所以从没覆盖到 |

### 8. 接入训练并启动正式训练
把 Route A 挂到 NAVSIM 训练流程里，**冻结主体、只训练压缩模块**。启动时验证通过的关键行：

```
[route-a] attached: trainable route_a=177.48M trainable dit=177.48M
```

两个数字相等，说明优化器**只看到 Route A 的参数**，主体确实被冻住了。

启动过程中排掉 5 个阻塞（都是几十秒就失败的，没浪费 GPU 长跑）：环境变量 `PYTHON` 没设导致缺 PyYAML；数据清单与数据目录不同源；部分场景没有 route 导致取不到训练窗口；4090 需要关掉 NCCL 的 P2P/IB；以及配置里写死的数据路径已经过期。

### 9. 一个对照实验，推翻了中途给出的结论
为了回答"训练之后 prune 与 register 差别还大不大"，我跑了一个**匹配 token 数量**的对照实验（同样的保留比例，一种做法是"删除"、另一种是"平均合并"）。结果（1024 个场景）：

| 做法 | PDM | 相对基线 |
|---|---:|---:|
| 不压缩 | 0.911205 | — |
| **删除**（prune） | 0.830022 | −0.081 |
| 平均合并（register 的近似） | 0.569067 | −0.342 |
| 合理分组后合并 | 0.589208 | −0.322 |

配对比较：合并比删除**低 0.24～0.26 PDM**，置信区间不含 0。

**这与我中途基于另一组数据给出的判断相反。** 中途我在保留比例 0.5 上看到"合并更好"，据此说"同预算下聚合保留更多信息"。在更激进的 0.25 上结论完全反转，而且合并的 PDM 掉到 0.57（接近乱开）。

**修正后的说法**：
- "聚合在数学上是选择的上界（能表达更多）"只是一句**理论上的表达力陈述，不预测实际效果**；
- 未经训练的合并可以**远差于**删除；
- 这个优势强烈依赖压缩比例，而且在激进压缩端反向；
- 因此 **Route B（register）的理由不能建立在"聚合本身更好"上**，只能建立在两点：① 用**可学习**的压缩器（不必是平均）；② 从第 0 层就开始压缩（同样 token 数下省 84.9%，而 Route A 只省 33.9%）。

---

## 四、我犯的错误与更正（如实列出）

| 错误 | 更正 |
|---|---|
| 一度声称仿真"证明打分器学会了选择" | 补做未训练对照后不成立，已在报告与交接文件中更正为"只证明机制、不证明学会" |
| 误报"推送失败、2 个改动未上传" | 实际当时已推送成功，是我用 `grep` 过滤输出时只看到失败行。只剩交接文件那一个改动未推 |
| 给的 MVP 实验 ETA 是 3 小时 | 实际按实测约 22 小时，已砍成单卡 1024 场景约 1.8 小时 |
| 两次没先做小规模验证就起长跑，浪费约 40 分钟 GPU | 一次是合并算子用法不对（7 个臂全部无效才发现），一次是参数与矩阵互斥。此后改成"先 2 场景 smoke 再起长跑" |
| 队列的完成判定写错，把 3 个已成功的臂标成 MISSING | 已修（运行器会创建 `_rerunNN` 后缀目录，判定必须按任意深度匹配） |

---

## 五、当前状态

### 正在运行
**Route A A1 正式训练**（tmux `driveva_routea`，GPU 4）

```
进度    : 约 640 / 3768 步（17%），1.05 步/秒，剩余约 50 分钟
配置    : 第 18 层压缩 / 3768 场景 / 1 轮 / 置零写法 / 主体冻结
损失    : 平均 2.27（前 150 步）→ 约 1.2（450-600 步），有波动但在下降
已存盘  : step-250、step-500（各 377 MB）
```

### 未完成
**PDM 评测还没做。** 训练完成只是第一步，还需要在 1024 个场景上评测，才能知道 Route A 到底有没有把 PDM 抬起来。

另外提醒一个已知的训练/推理差异：A1 用的是"置零"写法（为了让梯度能传回去），而部署时是真正把序列变短。两者有差距，计划里要求最后再用"真正变短"的写法微调一段（`--physical-shortening-final-steps`）。

### 卡住的事
**Git 推送失败**。VSCode 的凭据失效，本机领先远端 2 个提交（代码提交其实已经推上去了，只差记录文件）。按你的要求只试一次、失败即上报，已写进交接文件，**没有反复重试**。

### 资源
- GPU 4：Route A 训练中
- 其余卡：已被他人占用或空闲
- 全程遵守"最多 2 张卡、只用真正空闲的卡（≥40 GiB）"

---

## 六、下一步（按优先级）

1. **等训练结束，先做 1024 场景评测**——这是唯一能回答"Route A 有没有用"的实验。若 PDM 下界仍远低于 −0.002，说明第 18 层压缩的地板仍然太高，应把压缩起点往后挪再试。
2. **补一段"真正变短"的微调**，把 A1 的训练/推理差异收敛掉，否则评测会低估或高估。
3. **如果 Route A 摸到门槛**：继续把起点从 18 推进到 15、12，收益从 33.9% 涨到 50.9%；并做一遍"从 WAN2.2 初始化"的对照，区分"任务本身需要这么多信息"还是"DriveVA 训练方式造成的锁定"。
4. **如果 Route A 摸不到门槛**：把资源投到 Route B，但理由要用上面修正后的那两点（可学习压缩器 + 从第 0 层压缩），不要再拿"聚合更好"当依据。
5. Git 凭据恢复后推送并修正交接文件顶部的 HEAD 行。

---

## 七、本会话产出的文件

```
ROUTE_A_IMPLEMENTATION_AND_TRAINING_REPORT.md      # Route A 实现与训练报告（详细版）
videopress_framework/videopress/retraining/        # 7 个模块（开关/打分器/还原器/蒸馏/体检/总装/课表）
videopress_framework/tests/test_retraining_route_a.py   # 59 个单元测试
videopress_framework/scripts/train_route_a_smoke.py     # 训练驱动（仿真 + 守卫的真实入口）
videopress_framework/scripts/route_a_budget_report.py   # 成本模型
videopress_framework/scripts/route_a_sim_control.py     # 未训练对照（用于校准仿真结论）
videopress_framework/scripts/route_a_real_forward_smoke.py  # 真实模型接线验证
diffsynth/pipelines/wan_video_new.py               # Route A 接入点（守卫式，不影响既有路径）
examples/wanvideo/driveva_train/train_navsim_v1.py     # 训练侧接入（环境变量配置）
examples/wanvideo/driveva_train/scripts/train_navsim_v1.sh  # 数据路径修正
AGENTS.md                                          # 交接文件（含推送受阻说明）
```

---

# 附：训练完成后的评测结果（2026-09-24 晚）

## 一、训练本身是成功的

```
tmux driveva_routea : STAGE2 END rc=0, queue done
检查点             : 16 个（step-250 … step-3768，raw + EMA）
关键验证           : trainable route_a == trainable dit == 177.48M（主体确实冻结）
```

**运行机制全部正常**：真实模型前向、冻结主体、只训练压缩模块、存盘、断点续跑都验证通过。

## 二、评测结果：Route A 的 A1 检查点远差于基线

1024 场景、4 卡、同一批场景配对：

| 设置 | PDM | 实际保留 video token |
|---|---:|---:|
| **不压缩（基线）** | **0.911078** | 1560 |
| Route A，最终检查点，τ=0.5 | **0.540180** | 788（50%） |
| **配对 ΔPDM** | **−0.3709** | — |

过程中三次读数（同一面板家族，用于定位）：

| 检查点 | 上限夹 | PDM | 保留数 |
|---|---|---:|---:|
| step-1250 | 780 | 0.7336 | 780（被夹住） |
| step-1250 | 无 | 0.7443 | **1554.9（99.7%，几乎没压）** |
| step-3768 | 无 | 0.5402 | 788（50%） |

## 三、关键结论：这次负面结果**不能用来判定 Route A**

因为查下来是我自己的 A1 配置有三处错误：

1. **完全没有加稀疏压力项。** 我把 Route A 挂到了标准 FM 训练循环上，而那个循环只有轨迹/视频流匹配损失，**没有 `λ_sparse`**。后果有两个：
   - 没有任何力量推动"少留 token"——所以 step-1250 时保留率是 99.7%，压缩几乎没发生；
   - 也没有任何东西**锚定**保留率——所以门控分数随 FM 梯度自由漂移，到 step-3768 时自己漂到了 50%。保留率从 99.7% 变到 50% **不是被设计控制的，是失控的**。
2. **无意中把轨迹模块也训了。** 我只冻结了 `dit`，`trajectory_encoder` / `trajectory_head` 仍是可训练的（检查点里有它们 16 个张量为证）。而第一次评测**只加载了 Route A 权重**，等于拿"新压缩模块 + 旧轨迹头"去做评测——训练/评测不匹配，会单独造成很大跌幅。已修（现在会一起加载）。
3. **没有监控保留率。** 我写的 `CompressionStatsRecorder.is_truly_dynamic` 正是为了抓这种漂移，但**没有接进训练循环**。计划 §34–35 明确要求记录每场景/每轮 token 数，我漏了。

另外还有一个观察值得记下：**在 99.7% 保留（几乎不压缩）时 PDM 也只有 0.744**。但这一读数同样被第 2 条污染，所以**还不能**据此断定"插入门控+还原器本身就掉 0.17"。要在修好第 2 条后重测才能分清。

## 四、下一步必须做的修正（清单）

| 优先级 | 修正 | 理由 |
|---|---|---|
| P0 | 训练损失加上 `λ_sparse`（用 `compute_route_a_loss`，含稀疏项与 KD 项） | 没有它就既不会压缩、也无法锚定保留率 |
| P0 | 把 `CompressionStatsRecorder` 接进训练循环，每个 log 间隔打印 mean/P10/P50/P90 保留数 + `is_truly_dynamic` | 本次失败正是因为没人看这个数 |
| P0 | 冻结**除 Route A 以外的一切**（含 trajectory_encoder/head），或明确把它们纳入要加载的集合 | 避免训练/评测不匹配 |
| P1 | 按计划 §14 分阶段：A1 用 dense-gate 只训打分器+还原器 → 最后若干步切 `physical_shortening=True` 收尾 | 现在训练与推理的机制不同（置零 vs 真删），差距未收敛 |
| P1 | 阈值标定（计划 §25）：训练后在高保留端扫 τ，报告 PDM vs 保留数曲线 | 本次因上限夹写死导致第一次扫描无效，已修正为无夹扫描 |
| P2 | 用同一保留率跟**冻结剪枝**对比（例如都 50%），隔离"排序能力"与"机制代价" | 现在分不清是排序差还是机制差 |

## 五、需要说明的方法论教训（我自己犯的）

- **两次扫描设计错误**：第一次 τ 扫描忘了 `max_kept_total` 会把保留率钉死在 780，3 个臂等于白跑；我是在加了埋点、量到 `kept_video=780` 之后才发现。**教训：先量出被控变量，再设计扫描。**
- **两次 `pkill` 模式匹配到自己的命令行**，把当前 shell 杀掉（第二次导致队列脚本没写成）。**教训：用文件写入工具而不是 heredoc，避免模式自匹配。**
- 顺序上也走了弯路：应当**先**确认"保留率受控"再评测；我是在评测出异常数字后才回头补埋点。更省的顺序是：训练时就把保留率打进日志。

## 六、当前资源状态

- 所有 GPU 任务已结束（训练完成、评测完成），占用已释放
- 评测产物：`videopress_framework/outputs/route_a_eval_20260924/`
- 训练产物：`videopress_framework/outputs/route_a_train_20260924/a1_l18_densegate/`
- Git：本地 3 个提交未推送（VSCode 凭据失效，按用户要求未反复重试）

---

# 2026-09-25 附录：P0 修正落地，以及"门控根本不可用"的发现

## 一、这次做了什么

上一份报告列出的 P0 修正清单已经全部实现，并且在真实权重上做接线冒烟时
**又发现了三个更根本的缺陷**。结论一句话：

> Route A 现在失败的原因**不是"选不准"，而是门控（gate）本身还不可用**。

## 二、P0 清单完成情况

| 项 | 状态 | 说明 |
|---|---|---|
| 冻结除 Route A 以外的一切 | 完成 | 旧代码只冻结 `dit`，而 `--trainable_models` 默认还包含 `trajectory_encoder,trajectory_head`。现改为冻结整个 pipeline，并在挂载时**断言**没有其它可训练子模块（不满足直接抛错） |
| 训练损失加稀疏项 / KD | 完成 | `λ_sparse` + 与冻结 dense teacher 的轨迹流 KD（配对：同视频噪声、同轨迹噪声、同 timestep） |
| 接入保留率统计 | 完成 | `CompressionStatsRecorder` + `is_truly_dynamic`，每 N 步写 JSONL，结束时写 summary 与 calibration |
| 训练偏向规划而非视频重建 | 完成 | `trajectory_loss_scale`（视频损失对 780 个 token 取均值，轨迹只有 ~8 个点，不重加权时打分器实际在优化视频重建） |
| A1 尾声切 `physical_shortening=True` | 已排入队列 | 队列第 2 阶段：从第 1 阶段最后一个检查点热启动跑 1 epoch |
| τ 标定 | 完成但**换了做法** | 见下 |
| 与同保留率冻结剪枝对比 | 待训练跑完后做 | — |

框架测试：**338 passed**（新增 15 个）。

## 三、最重要的发现：门控根本不可用

我用真实权重做小规模冒烟（8–40 个场景），逐步定位：

1. **固定 τ=0.5：门控在刀刃上。**
   scorer 刚初始化时分数集中在 0.5、标准差只有 0.03。**一个优化步就让硬门保留率
   从 54.1% 掉到安全下限 6.2%**，而反向传播用的软掩码还停在 0.44。
   也就是说：**前向真正执行的硬门，和反向用来学习的软代理，已经不是同一个东西了。**

2. **改成 τ 反馈控制 → 变成开关式震荡。**
   保留率是 τ 的近似阶跃函数（1600 个候选、分数标准差 0.026，整个过渡带只有约 0.1，
   回路增益约 10/单位 τ）。实测 history 卡在下限 0.041 而 τ 单调往上爬，
   future 在相邻两步之间 0.082 ↔ 1.000 来回跳。这个控制器我删掉了，不留死代码。

3. **改成"按分位数标定 τ" → 仍然 0%/100% 跳。**
   把 τ 设成分数的 75% 分位数后，保留率仍然是 0.026 / 0.46 / 1.00 / 0.29 这样跳。
   原因找到了：**scorer 的分数被"每个场景的常数偏移"支配**——把 1560 个候选的分数
   池化起来标准差只有 0.023，而场景之间的均值漂移是同一个量级。
   所以任何**全局** τ 都只能落在某个场景整团分数的上面或下面，非 0 即满。

4. **修复：阈值之前先做分数标准化。**
   对**每个场景、每个 domain** 的 logits 做 z-score（`normalize_scores`），
   再和 τ 比较。复测结果：保留率稳定在 **0.22–0.28**（目标 0.25），
   分数均值 0.50 / 标准差 0.20，τ 稳定在 0.6719，不再跳变。
   我补了单元测试把这个回归钉住（同一个分数整体平移 ±4，标准化后保留数完全一样；
   不标准化时门控看不见排序）。

## 四、这样修的代价（必须写清楚，不能藏）

- 分数标准化之后，保留率只取决于分数分布的**形状**，所以 **K 近似是常数**，
  **场景自适应长度不再由门控产生**。冒烟里 `is_truly_dynamic` 仍报 True，
  但 P10≈357 / P90≈417，只能算"弱动态"。计划书里的 "scene-dependent K"
  用这条路**还没有实现**，需要等 A2/A3 解冻后做绝对尺度标定，或者加一个
  场景难度预测头。
- 标准化之后"整体平移"对门控**完全没有影响**，所以计划里的 `L_sparse`
  （`mean(s_i)`）对预算**不再是有效旋钮**，只会在形状上施加扭曲。
  因此这一轮把 `lambda_sparse_max=0`（关闭），代码路径保留给未标准化的配方。
  **这是对计划书 §5/§30 的一处有意偏离**，理由就是上面第 1–3 条的实测。
- 因此**本轮 A1 只回答一个问题**：给定固定保留率，训练出来的 scorer 排序是否优于随机。
  它**不回答**"动态长度"。

## 五、当前状态

- **重跑队列已排队**：`videopress_framework/outputs/route_a_train2_20260924/run_queue.sh`，
  持久 tmux 会话 `route_a2`。
  - 阶段 0：8 场景接线冒烟（1 卡）；
  - 阶段 1：4 卡 A1 dense-gate，3,768 场景 × 8 epoch；
  - 阶段 2：从阶段 1 最后检查点热启动，切 `physical_shortening=True` 跑 1 epoch。
- **阻塞是 GPU，不是代码**：从 2026-09-24 21:32 起 8 张卡被他人
  （maqianli 的 8 个 vLLM worker）占满，每张只剩 1.4–9.4 GiB 空闲
  （项目规则要求 ≥40 GiB，且不与他人共卡）。队列按标准自行等待。
- 顺带清理：删掉了自 2026-09-20 起一直空等 GPU 的旧 `token_oracle` 队列会话
  （future hard prune 已判决终止，那个队列只会抢卡）。
- 磁盘：根分区只剩 27 GB，所以本轮把保存间隔放宽到 1000 步（约 8 个检查点）。

## 六、又一条操作教训

`pkill -f <模式>` 会匹配到**自己这条命令行**。本会话（含前一段）已经两次因此把
自己的 shell 杀掉。**以后一律用 `pgrep` 拿到显式 PID 再 kill**，
或者让模式带一个不会自我匹配的字符类。

